#labels Featured,Phase-Deploy
  # Download the latest release from the downloads tab.
  # Extract to the hardware/libraries directory in your Arduino install directory.
  # Compile by either reloading the Arduino IDE or selecting the board again.